/** Centralized icon sizes — adjust values here to tune globally */
export const ICON_SIZE = {
  /** Transport buttons (Play, Pause, Skip, Rewind) */
  TRANSPORT: 15,
  /** Inline action buttons (Close, Edit, Delete, Loop) */
  ACTION: 10,
  /** Accordion expand/collapse arrows */
  ACCORDION: 12,
  /** Decorative/label icons (Music, Mixer, File indicators) */
  LABEL: 12,
} as const;